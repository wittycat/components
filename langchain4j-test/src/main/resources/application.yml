server:
  # ai-agent 占 8080、agentscope-test 占 8081、langgraph4j-test 预留 8082,本模块的 Web SSE 示例用 8083
  port: 8083

# GLM 模型配置的默认值(可提交 git)。
# 真实 api-key 放在 application-local.yml(已被 .gitignore 排除),不要写在这里。
# 读取优先级:-D 系统属性 > 环境变量 > application-local.yml > 本文件
glm:
  api-key: demo
  base-url: https://open.bigmodel.cn/api/coding/paas/v4
  model: glm-5.3-flash
  embedding-model: embedding-3
