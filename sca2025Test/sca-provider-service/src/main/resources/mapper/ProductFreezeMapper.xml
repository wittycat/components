<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.wittycat.components.sca.provider.mapper.ProductFreezeMapper">

    <!--根据用户ID查询用户的账户信息-->
    <insert id="insert" parameterType="com.wittycat.components.sca.provider.entity.ProductFreeze">
        insert into product_freeze(XID, USER_ID, FREEZE_NUM, STATE)
        values (#{xid,jdbcType=VARCHAR},
                #{userId,jdbcType=INTEGER},
                #{freezeNum,jdbcType=INTEGER},
                #{state,jdbcType=INTEGER})
    </insert>

    <!--根据id删除冻结记录-->
    <update id="deleteById" parameterType="java.lang.String">
        delete
        from product_freeze
        where xid = #{xid,jdbcType=VARCHAR};
    </update>


    <!--修改冻结记录-->
    <update id="updateById" parameterType="com.wittycat.components.sca.provider.entity.ProductFreeze">
        update
            product_freeze
        set freeze_num= #{freezeNum,jdbcType=INTEGER},
            state= #{state,jdbcType=INTEGER}
            where xid = #{xid,jdbcType=VARCHAR};
    </update>

    <!--查询freeze中是否有冻结记录-->
    <select id="selectById" parameterType="java.lang.String" resultType="com.wittycat.components.sca.provider.entity.ProductFreeze">
        select *
        from product_freeze
        where xid = #{xid,jdbcType=VARCHAR};
    </select>
</mapper>