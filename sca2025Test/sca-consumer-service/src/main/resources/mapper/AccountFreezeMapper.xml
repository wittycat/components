<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.account.mapper.AccountFreezeMapper">

    <!--根据用户ID查询用户的账户信息-->
    <insert id="insert" parameterType="com.wittycat.components.sca.consumer.entity.AccountFreeze">
        insert into account_freeze(XID, USER_ID, FREEZE_MONEY, STATE)
        values (#{xid,jdbcType=VARCHAR},
                #{userId,jdbcType=INTEGER},
                #{freezeMoney,jdbcType=DOUBLE},
                #{state,jdbcType=INTEGER})
    </insert>

    <!--根据id删除冻结记录-->
    <update id="deleteById" parameterType="java.lang.String">
        delete
        from account_freeze
        where xid = #{xid,jdbcType=VARCHAR};
    </update>


    <!--修改冻结记录-->
    <update id="updateById" parameterType="com.wittycat.components.sca.consumer.entity.AccountFreeze">
        update
            account_freeze
        set freeze_money= #{freezeMoney,jdbcType=DOUBLE},
            state= #{state,jdbcType=INTEGER},
        where xid = #{xid,jdbcType=VARCHAR};
    </update>

    <!--查询freeze中是否有冻结记录-->
    <select id="selectById" parameterType="java.lang.String" resultType="com.wittycat.components.sca.consumer.entity.AccountFreeze">
        select *
        from account_freeze
        where xid = #{xid,jdbcType=VARCHAR};
    </select>
</mapper>